<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.multi.datasource.demo.mapper.OrderMapper">

    <select id="findOrderInfoById" resultType="com.example.multi.datasource.demo.entities.OrderEntity" parameterType="integer">
        SELECT * FROM t_order where id = #{id}
    </select>

</mapper>