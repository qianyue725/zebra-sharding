<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zebra.multisource.demo.mapper.OrderMapper">

    <select id="findOrderInfoById" resultType="com.example.zebra.multisource.demo.entities.OrderEntity" parameterType="integer">
        SELECT * FROM t_order where id = #{id}
    </select>

</mapper>