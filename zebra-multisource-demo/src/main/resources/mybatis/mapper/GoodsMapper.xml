<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.zebra.multisource.demo.mapper.GoodsMapper">

    <select id="findGoodsInfoById" resultType="com.example.zebra.multisource.demo.entities.GoodsEntity" parameterType="integer">
        SELECT * FROM t_goods where id = #{id}
    </select>

</mapper>